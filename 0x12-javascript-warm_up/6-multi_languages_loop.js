#!/usr/bin/node
// Shebang for Node.js
const lines = ['C is fun', 'python is cool', 'JavaScript is amaizing'];

// Loop through the array and print each line
for (let i = 0; i < lines.length; i++) {
	console.log(lines[i]);
}
