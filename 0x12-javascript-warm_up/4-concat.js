#!/usr/bin/node
// Shebang line to specify the Nod.js

// first user argument
const arg1 = process.argv[2];

// second user argumen.
const arg2 = process.argv[3];

// Print the two args in the require format.
console.log(`${arg1} is ${arg2}`);
